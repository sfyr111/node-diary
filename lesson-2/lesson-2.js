/**
 * > path.parse(process.cwd())
 * { root: '/',
 *   dir: '/Users/yangran1/workspace',
 *   base: 'node-diary',
 *   ext: '',
 *   name: 'node-diary' }
 */

/**
 * utf-8 读文件
 * base64 图片
 * binary 二进制 读图片
 * fs.readFile('./', 'binary', (err, data) => {}) 这个是异步I/O 
 * fs.readFileSync 同步
 */